// Shared Types

export interface Task {
  id: string;
  text: string;
  completed: boolean;
}

export interface Colors {
  bg: string;
  surface: string;
  border: string;
  accent: string;
  accentLight: string;
  success: string;
  danger: string;
  textPrimary: string;
  textSecondary: string;
  textMuted: string;
}
